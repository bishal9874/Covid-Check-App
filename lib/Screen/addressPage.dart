import 'package:flutter/material.dart';

class AddresssPage extends StatelessWidget {
  final double totalamount;
  const AddresssPage({Key key, this.totalamount}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
